# Example environment variables for the backend
# Copy this to `.env` and set real values before running in production

# Either set the full URI directly:
# MONGODB_URI=mongodb+srv://zyxcab101819_db_user:<db_password>@websites.lvxthbm.mongodb.net/?appName=Websites

# Or set the password and the code will build the Atlas URI automatically
DB_USER=zyxcab101819_db_user
DB_PASSWORD=your_db_password_here
DB_HOST=websites.lvxthbm.mongodb.net
DB_APP_NAME=Websites

PORT=5000
JWT_SECRET=replace_with_a_secure_secret
